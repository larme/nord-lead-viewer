// Generated by CoffeeScript 1.3.1
(function() {
  var bang, inlets, msg_counter, msg_int, outlets, parse_raw_data, patch_counter, raw_data, root, status, sysex_finished;

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  inlets = 1;

  outlets = 1;

  patch_counter = 0;

  msg_counter = 0;

  raw_data = [];

  status = 0;

  msg_int = function(n) {
    var temp_data;
    switch (n) {
      case 240:
        status = 1;
        return temp_data = [];
      case 247:
        status = 0;
        return sysex_finished(raw_data);
      default:
        return raw_data.push(n);
    }
  };

  bang = function(b) {
    return post("love");
  };

  sysex_finished = function(data) {
    var elt, i, result, _i, _len, _results;
    result = parse_raw_data(data);
    if (result) {
      _results = [];
      for (i = _i = 0, _len = result.length; _i < _len; i = ++_i) {
        elt = result[i];
        _results.push(outlet(0, [i + 1, elt]));
      }
      return _results;
    }
  };

  parse_raw_data = function(data) {
    return data;
  };

  root.inlets = inlets;

  root.outlets = outlets;

  root.bang = bang;

  root.msg_int = msg_int;

}).call(this);
